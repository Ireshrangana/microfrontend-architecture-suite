name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add real install/build/test steps for the production-single-spa workspace"
