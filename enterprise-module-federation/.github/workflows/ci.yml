name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add real pnpm install/build/test steps for the enterprise workspace"
